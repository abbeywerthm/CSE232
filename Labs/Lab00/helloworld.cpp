/* NOTES:
always do this:
git add .
git commit -m "adding __name of file__"
git push

AND THEN
COMMAND + S (to save)

so then it will make sure to add to github for it
to run properly */

/* How to run terminal:
g++ -std=c++20 -Wall {your file's name}.cpp
*/




#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}

